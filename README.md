# employee-management-servlet-crud
A Java Servlet based Employee Management System implementing CRUD operations using JDBC, JSP, JSTL, and Layered Architecture (Controller → Service → DAO → Model). Built to practice enterprise-level code structure and separation of concerns.
